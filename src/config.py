"""
Loads configs/*.yaml and exposes paths + constants to every notebook.

Functions to implement:
- load_config(name: str) -> dict         # reads one configs/<name>.yaml
- load_all_configs() -> dict[str, dict]  # reads all 5 config files
"""
